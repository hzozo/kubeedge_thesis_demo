apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    k8s-app: kubeedge-hudtemp-app
  name: kubeedge-hudtemp-app
  namespace: default
spec:
  selector:
    matchLabels:
      k8s-app: kubeedge-hudtemp-app
  template:
    metadata:
      labels:
        k8s-app: kubeedge-hudtemp-app
    spec:
      hostNetwork: true
      nodeSelector:
        node-role.kubernetes.io/master: ""
      containers:
      - name: kubeedge-hudtemp-app
        image: hzozo/kubeedge-virt-hudtemp-mapper:v0.3.1
        imagePullPolicy: IfNotPresent
      tolerations:
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule
      restartPolicy: Always
---
apiVersion: rbac.authorization.k8s.io/v1beta1
kind: Role
metadata:
  name: kubeedge-counter
  namespace: default
rules:
- apiGroups: ["devices.kubeedge.io"]
  resources: ["devices"]
  verbs: ["get", "patch"]

---
apiVersion: rbac.authorization.k8s.io/v1beta1
kind: RoleBinding
metadata:
  name: kubeedge-counter-rbac
  namespace: default
subjects:
  - kind: ServiceAccount
    name: default
roleRef:
  kind: Role
  name: kubeedge-counter
  apiGroup: rbac.authorization.k8s.io